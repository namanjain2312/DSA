1>Inorder Traversal - Left , Root , Right
2>PreOrder Traversal - Root , Left , Right
3>PostOrder Traversal - Left , Right , Root
4>directed graph - a graph in which the edges have a direction
5>connected graph - a graph where every pair of vertices is connected by a path
In other words, you can reach any vertex from any other vertex by following the edges of the graph

8> In a directed graph, a
Strongly Connected Component is a subset
of vertices where every vertex in the subset is reachable from every other
vertex in the same subset by traversing the directed edges.
9>Linear Graph with no cycle are always bipartite graphs
10>Any graph with even cycle lenght are  bipartite graphs
11>Any graph with odd cycle lenght are not bipartite
12>We cannot use dijiktra algortihm for the negative weights
13>Bellman Ford is used for only directed graph , if we have been provided with a undirected graph ,
then convert it into a directed graph
14>Diff bw dijaktra and bellman ford is bellman ford can we implemented for negiative weightes and negative cyclw as well
15>Perfect Binary Tree : where all leaves are on the same level, and every parent has two children.
16>A height-balanced binary tree is a binary tree in which the depth of the
two subtrees of every node never differs by more than one.
17>A binary search tree is a binary tree where for every node, any descendant of Node.left
has a value less than Node.val, and any descendant of Node.right has a value
greater than Node.val
18>InOrder Traversal of an BST is always a ascending array value
19>A spanning tree is defined as a tree-like subgraph of a connected,
undirected graph that includes all the vertices of the graph. Or, to say in Layman’s words,
it is a subset of the edges of the graph that forms a tree (acyclic) where every node of the
 graph is a part of the tree.
20>The minimum spanning tree has all the properties of a spanning tree with an added constraint of having the minimum possible weights
among all possible spanning trees. Like a spanning tree, there can also be many possible MSTs
for a graph.
21>To make an structuallay unique binary tree you always need an inorder + preorder/postorder , via
inordder we decide left and right subtree anf via post/preorder we define the placement of root.
22>If only pre and post order given then we cannot make structuraaly unique bainry tree.
23>

