class Solution {
public:
    void rotate(vector<vector<int>>& matrix) {
        int n,m;
        n=matrix.size();
        m=matrix[0].size();
        //transpose
        for(int i=0;i<n-1;i++)
        {
            for(int j=i+1;j<m;j++)
                swap(matrix[i][j],matrix[j][i]);
        }
        for(int i=0;i<n;i++)
        {
            reverse(matrix[i].begin(),matrix[i].end());
        }
    }
};