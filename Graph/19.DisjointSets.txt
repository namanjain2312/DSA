1>So basically let say we have a grpah
1->2->4->6
and
5->9->8
Now does 1 and 2 belongs to same set , now for checking the same we need to dfs on graph first and then return
the answer as true if both find and false if only one is found .So it will take O(V+E) complexity ,
but with disjoint set we can do it in constant time
2>Basicallt disjoint set are mostly used in dynamci grpah , graoh which changes its configuration
frequently
3>Two main funcationlity of disjoint set
    a>Find Parent
    b>Find Union
        i>Via Rank
        ii>Via Size



Union Via Rank
Steps
1>Take a rank array of size V (Intilize each rank[i] = 0)
2>Take a parent array of size V (Intialize each parent[i] = i)
3>Union(u,v)
    i>Find ultimate parent of u and v (Pu and Pv)
    ii>Find rank of Pu and Pv
    iii>Connect smaller rank to larger rank always
    iv>Do path compression on the way
    v>So first time finding parent will take O(log E) , if we do path compression then next time it
    would take only O(1)
    vi>So Time Conpleixyt of
        a>Find Parent = 4 * alpha = const
        b>Union (u,v) = 4 * alpha = const
    vii>Why only connect smaller rank with greater not vice versa , as if we connect the longer path
    graph under small rank , time to reach that last node will increase by one and thus unecessary increase
    in height(However connecting small rank group under high rank will also increase time to visit node of
    small rank by one , but number of nodes are less than as compared to alarger graph)





Code Of Union By Rank

#include <bits/stdc++.h>
using namespace std;
class DisjointSet {
    vector<int> rank, parent;
public:
    DisjointSet(int n) {
        rank.resize(n + 1, 0);
        parent.resize(n + 1);
        for (int i = 0; i <= n; i++) {
            parent[i] = i;
        }
    }

    int findUPar(int node) {
        if (node == parent[node])
            return node;
        return parent[node] = findUPar(parent[node]);
    }

    void unionByRank(int u, int v) {
        int ulp_u = findUPar(u);
        int ulp_v = findUPar(v);
        if (ulp_u == ulp_v) return;
        if (rank[ulp_u] < rank[ulp_v]) {
            parent[ulp_u] = ulp_v;
        }
        else if (rank[ulp_v] < rank[ulp_u]) {
            parent[ulp_v] = ulp_u;
        }
        else {
            parent[ulp_v] = ulp_u;
            rank[ulp_u]++;
        }
    }
};
int main() {
    DisjointSet ds(7);
    ds.unionByRank(1, 2);
    ds.unionByRank(2, 3);
    ds.unionByRank(4, 5);
    ds.unionByRank(6, 7);
    ds.unionByRank(5, 6);
    // if 3 and 7 same or not
    if (ds.findUPar(3) == ds.findUPar(7)) {
        cout << "Same\n";
    }
    else cout << "Not same\n";

    ds.unionByRank(3, 7);

    if (ds.findUPar(3) == ds.findUPar(7)) {
        cout << "Same\n";
    }
    else cout << "Not same\n";
    return 0;
}


Union Via Size
Steps
1>Take a size array of size V (Intilize each size[i] = 1)
2>Take a parent array of size V (Intialize each parent[i] = i)
3>Union(u,v)
    i>Find ultimate parent of u and v (Pu and Pv)
    ii>Find size of Pu and Pv
    iii>Connect smaller size to larger size always
    iv>Do path compression on the way
    v>So first time finding parent will take O(log E) , if we do path compression then next time it
    would take only O(1)
    vi>So Time Conpleixyt of
        a>Find Parent = 4 * alpha = const
        b>Union (u,v) = 4 * alpha = const
    vii>Why only connect smaller size with greater not vice versa , as if we connect the longer path
    graph under small size , time to reach that last node will increase by one and thus unecessary increase
    in height(However connecting small size group under high size will also increase time to visit node of
    small size by one , but number of nodes are less than as compared to a larger graph)





Code Of Union By size


#include <bits/stdc++.h>
using namespace std;
class DisjointSet {
    vector<int> rank, parent, size;
public:
    DisjointSet(int n) {
        rank.resize(n + 1, 0);
        parent.resize(n + 1);
        size.resize(n + 1);
        for (int i = 0; i <= n; i++) {
            parent[i] = i;
            size[i] = 1;
        }
    }

    int findUPar(int node) {
        if (node == parent[node])
            return node;
        return parent[node] = findUPar(parent[node]);
    }

    void unionByRank(int u, int v) {
        int ulp_u = findUPar(u);
        int ulp_v = findUPar(v);
        if (ulp_u == ulp_v) return;
        if (rank[ulp_u] < rank[ulp_v]) {
            parent[ulp_u] = ulp_v;
        }
        else if (rank[ulp_v] < rank[ulp_u]) {
            parent[ulp_v] = ulp_u;
        }
        else {
            parent[ulp_v] = ulp_u;
            rank[ulp_u]++;
        }
    }

    void unionBySize(int u, int v) {
        int ulp_u = findUPar(u);
        int ulp_v = findUPar(v);
        if (ulp_u == ulp_v) return;
        if (size[ulp_u] < size[ulp_v]) {
            parent[ulp_u] = ulp_v;
            size[ulp_v] += size[ulp_u];
        }
        else {
            parent[ulp_v] = ulp_u;
            size[ulp_u] += size[ulp_v];
        }
    }
};
int main() {
    DisjointSet ds(7);
    ds.unionBySize(1, 2);
    ds.unionBySize(2, 3);
    ds.unionBySize(4, 5);
    ds.unionBySize(6, 7);
    ds.unionBySize(5, 6);
    // if 3 and 7 same or not
    if (ds.findUPar(3) == ds.findUPar(7)) {
        cout << "Same\n";
    }
    else cout << "Not same\n";

    ds.unionBySize(3, 7);

    if (ds.findUPar(3) == ds.findUPar(7)) {
        cout << "Same\n";
    }
    else cout << "Not same\n";
    return 0;
}