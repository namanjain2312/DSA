2.ImplementQueueUsingArrays

https://leetcode.com/problems/design-circular-queue/description/
https://www.naukri.com/code360/problems/implement-queue-using-arrays_8390825?leftPanelTabValue=PROBLEM


//Do it again with the best version , solve cycle one stack approach
//Easy Version

class Queue {

	int front, rear;
	vector<int> arr;

public:
	Queue()
	{
		front = 0;
		rear = 0;
		arr.resize(100001);
	}

	// Enqueue (add) element 'e' at the end of the queue.
	void enqueue(int e)
	{
		arr[rear]=e;
		rear++;
	}

	// Dequeue (retrieve) the element from the front of the queue.
	int dequeue()
	{
		if(front==rear)
			return -1;
		int val=arr[front];
		front++;
		return val;
	}
};